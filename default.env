# EasyClaw Default Configuration
# This file is committed to git and serves as a template.
# On first run, setup.sh copies this to .env and populates values.
# Edit .env after setup if you need to change settings.

# ====== SYSTEM ======
# System username that will run Claude Code and Telegram bot
# Default: current user (ubuntu)
SETUP_USER=ubuntu

# ====== CLAUDE SESSION ======
# Auto-generated cryptographically-secure session ID (32-char hex)
# This persists across VPS reboots to maintain Claude context
# Don't edit manually — setup.sh manages this
SESSION_ID=

# Directory where EasyClaw is installed (auto-set by setup.sh)
EASYCLAW_DIR=/path/to/easyclaw

# ====== TELEGRAM ======
# Telegram bot token from BotFather (@BotFather on Telegram)
# Format: 123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# Optional — leave as placeholder if you don't use Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Auto-registered on first message from user to bot (don't edit)
TELEGRAM_CHAT_ID=

# Directory where files sent via Telegram are saved (default: ~/telegram-files)
TELEGRAM_FILES_DIR=

# ====== BOT IDENTITY ======
# Name of your personal AI assistant (used in initial prompts)
BOT_NAME=Clawdy

# One-line purpose/role description
BOT_PURPOSE=your personal AI assistant

# ====== CLAUDE CONFIGURATION ======
# Default model: haiku (fast, cheap), sonnet (balanced), or opus (most capable)
# Can be overridden per-session with claude --model <model>
CLAUDE_DEFAULT_MODEL=haiku

# ====== BOT-TO-BOT BRIDGE (Tailscale) ======
# Shared secret key — must be the same on both machines
BRIDGE_API_KEY=

# Port the bridge HTTP server listens on (Tailscale interface only)
BRIDGE_PORT=8765

# Tailscale URL of the peer bot's bridge endpoint
# e.g. http://100.x.x.x:8765  (leave blank to disable outbound sends)
PEER_BRIDGE_URL=

# ====== ADVANCED ======
# Only change these if you know what you're doing

# Discord webhook (optional, for logging)
# DISCORD_WEBHOOK=

# Memory database path (optional, for custom memory storage)
# MEMORY_DB=/home/SETUP_USER/.easyclaw/memories.db

# Sentry error tracking (optional)
# SENTRY_DSN=

# Log level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO
